/**
 * Marks the header logo that the splash screen measures and flies to.
 * Lives in its own module so the server-rendered header can import it without
 * reaching into the splash's client bundle.
 */
export const BRAND_LOGO_ATTR = 'data-brand-logo'
